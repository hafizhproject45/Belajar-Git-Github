Cara git :
-Pwd ( untuk melihat directory sekarang )
-Cd ( untunk merubah directory )
-git init ( untuk menjadi lokal repo )


Cara pertama :
-git add  .
-git commit -m ""

Jika sudah modified :
-git commit -am ""

Liat graph branch :
Git log  --all --decorate --oneline --graph
( graph )

Pindah branch :
-Git checkout <nama_branch>

Chenk branch :
-Git branch

Cara liat yang dapat di delete :
-git branch --merged

Deletenya :
-git branch -d <nama_branch>

Three way merge :
-git merge <nama-branch>

Pindah branch ke yg sudah di hapus :
-git checkout <hash_7>

Git Remote :
-git remote -v ( untuk mengecek isi dari remote )

cara Remote :
-git init
-git add .
-git commit -m ""
--copy dari github baris satu <or push an existing repository from the command line>
--dan baris 2
